# Backend origin the panel talks to in production builds (`vite build`).
#
# Vite inlines every `VITE_*` value into the bundle, so this is a build-time
# setting: edit it and rebuild/redeploy — a running bundle cannot be repointed.
#
#   VITE_API_BASE_URL=/                     ← 面板和后端在同一个源后面（推荐）
#   VITE_API_BASE_URL=https://api.example   ← 面板与后端分处不同源
#   （不设置）                                ← 默认也是「同源」
#
# 生产部署方式是「面板静态文件 + /api 反代挂在同一个域名/端口后面」
# （见后端仓库 docker-compose.yml 的 terraria-panel 服务），所以这里用 `/`：
# 请求走相对路径，没有 Mixed Content（HTTPS 页面调 HTTP 接口会被浏览器拦），
# 也不需要 CORS。值不要带结尾斜杠。
#
# 例外：如果要把面板单独部署到别的源 —— 例如 Cloudflare Workers
# （`pnpm run deploy` → terraria-panel.tyrantblue32.workers.dev），
# 就必须在这里写死后端域名，否则 `/` 会让面板去请求 Workers 自己的 `/api/*`
# 而 404：
#   VITE_API_BASE_URL=https://terraria-api.tyrantblue.xyz
VITE_API_BASE_URL=/
