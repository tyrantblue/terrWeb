# Copy to `.env.local` (git-ignored) to override the backend for local work.
# Vite inlines `VITE_*` values at build time, so restart `pnpm dev` (or rebuild)
# after changing it.

# Backend origin, without a trailing slash. Defaults to
# https://terraria-api.tyrantblue.xyz when unset.
VITE_API_BASE_URL=http://117.72.197.18:8080
