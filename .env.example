# Copy to `.env.local` (git-ignored) to override the backend for local work.
# Vite inlines `VITE_*` values at build time, so restart `pnpm dev` (or rebuild)
# after changing it.

# Backend origin, without a trailing slash.
#   VITE_API_BASE_URL=/                     同源（默认；面板与 /api 同一域名/端口）
#   VITE_API_BASE_URL=https://api.example   另一个源
# 不设置时默认同源（window.location.origin）。
#
# 本地开发时面板跑在 Vite 的 5173 端口，后端在别的端口，所以通常要显式写：
#   VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_API_BASE_URL=http://127.0.0.1:8080
